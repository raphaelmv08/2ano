import { StyleSheet } from 'react-native';

export let styles = StyleSheet.create({
tituloNome: {
    fontSize: 22,
    fontWeight: 'bold',
    marginBottom: 25,
    textAlign: 'center',
    color: '#003366',   
  }
}); 
